import { writable } from "svelte/store";

export const changeFloor = writable(() => {});

export const showOverlay = writable(() => {});

export const showHud = writable(() => {});

export const hideHud = writable(() => {});

export const showBlocker = writable(() => {});
